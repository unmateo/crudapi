{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: CrudAPI",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "tests.server:app",
                "--reload"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
