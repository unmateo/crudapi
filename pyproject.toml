[tool.poetry]
name = "crudapi"
version = "0.1.0"
description = ""
authors = ["Mateo Harfuch <mateo.harfuch@tunubi.com>"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.61.1"
SQLAlchemy = "^1.3.20"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
requests = "^2.24.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
